## Q1. What is React?

React is a JavaScript library for building user interfaces, especially interactive web applications. It allows us to build the UI using small and reusable components. React also uses a state-driven approach, so when the application state changes, React updates the UI to match the new state. This makes large and interactive applications easier to build, maintain, and reuse.


